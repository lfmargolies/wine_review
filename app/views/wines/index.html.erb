<h1>Wines</h1>
<p>Wines Currently in stock as of <%= formatted_time(@available_at) %></p>
<ol>
<% @wines.each do |wine| %>
  <li>
  	<%= link_to(wine.name, wine) %>: <%= wine.year %> <%= wine.varietal %> bottled by <%= wine.winery %>, <%= wine.country %>
  </li>
<% end %>
</ol>